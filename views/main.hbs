<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
</head>
<body>
    <header>
        <h1>{{title}}</h1>
        <!-- Example of rendering pages from your array -->
        <nav>
            <ul>
                {{#each pages}}
                    <li><a href="{{url}}">{{title}}</a></li>
                {{/each}}
            </ul>
        </nav>
    </header>

    <main>
        {{{body}}} <!-- Content of the specific page will go here -->
    </main>

    <footer>
        <p>Footer Content</p>
    </footer>
</body>
</html>
